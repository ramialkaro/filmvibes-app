import 'package:flutter/material.dart';

class BookCinemaScreen extends StatelessWidget {
  const BookCinemaScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
